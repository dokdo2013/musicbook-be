import { Test, TestingModule } from '@nestjs/testing';

describe('datadog', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });

  // it('increment', () => {
  //   datadog.increment('test');
  // });
});
